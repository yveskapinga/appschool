<!DOCTYPE html>
<html>
<head>
    <title>Rapport des paiements</title>
    <style>
        /* Ajoutez ici vos styles CSS */
    </style>
</head>
<body>
    <header>
        <div style="text-align: left;">
            <img src="path/to/logo.png" alt="Logo de l'école">
            <p>Adresse physique de l'école</p>
            <p>Email de l'école</p>
            <p>Site internet de l'école</p>
            <p>Numéro de contact de l'école</p>
        </div>
        <div style="text-align: right;">
            <p>Date du jour : {{ "now"|date('Y-m-d') }}</p>
        </div>
    </header>

    <h1 style="text-align: center;">Rapport des paiements</h1>

    <table>
        <thead>
            <tr>
                <th>Élève</th>
                <th>Date</th>
                <th>Montant</th>
                <th>Mode de paiement</th>
                <th>Status</th>
                <th>Commentaire</th>
            </tr>
        </thead>
        <tbody>
            {% for paiement in paiements %}
            <tr>
                <td>{{ paiement.eleve.nom }} {{ paiement.eleve.prenom }}</td>
                <td>{{ paiement.createdAt|date('d/m/Y') }}</td>
                <td>{{ paiement.montant }}</td>
                <td>{{ paiement.modePaiement }}</td>
                <td>{{ paiement.status }}</td>
                <td>{{ paiement.commentaire }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
