{# templates/inscription/form.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    <h1> {% if new %}
        Inscription {% else %}
        Réinscription
    {% endif %}</h1>

    {{ form_start(form) }}
    {{ form_rest(form) }}

    <button type="submit" class="btn btn-succes"> {% if new %}
        inscrire {% else %}
        Réinscrire
        {% endif %} </button>
     {{ form_end(form) }}
{% endblock %}
