{% extends 'base.html.twig' %}

{% block title %}Enseignant{% endblock %}

{% block body %}
    <h1>Enseignant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseignant.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ enseignant.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ enseignant.roles ? enseignant.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ enseignant.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ enseignant.nom }}</td>
            </tr>
            <tr>
                <th>DateDeNaissance</th>
                <td>{{ enseignant.DateDeNaissance ? enseignant.DateDeNaissance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ enseignant.sexe }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ enseignant.adresse }}</td>
            </tr>
            <tr>
                <th>NumeroTelephone</th>
                <td>{{ enseignant.numeroTelephone }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ enseignant.photo }}</td>
            </tr>

            <tr>
                <th>CreatedAt</th>
                <td>{{ enseignant.createdAt ? enseignant.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>

            <tr>
                <th>Fonction</th>
                <td>{{ enseignant.fonction }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ enseignant.matricule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enseignant_index') }}">back to list</a>

    <a href="{{ path('app_enseignant_edit', {'id': enseignant.id}) }}">edit</a>

    {{ include('enseignant/_delete_form.html.twig') }}
{% endblock %}
