{% extends 'base.html.twig' %}

{% block title %}Créer élève{% endblock %}

{% block body %}
{{ form_start(form) }}
    <div class="row">
        <div class="col">{{ form_row(form.prenom) }}</div>
        <div class="col">{{ form_row(form.nom) }}</div>
        <div class="col">{{ form_row(form.postnom) }}</div>
    <div class="row">
        {# <div class="col">{{ form_row(form.DateDeNaissance | date('d/m/Y' )) }}</div> #}
        <div class="col">{{ form_row(form.adresse) }}</div>
        <div class="col">{{ form_row(form.photo, { label: 'Profil'}) }}</div>
        <div class="col">{{ form_row(form.DateDeNaissance) }}</div>
    </div>
    <div class="row">
        
        {# <div class="col">{{ form_row(form.DateDeNaissance | date('d/m/Y' )) }}</div> #}
        <div class="col">{{ form_row(form.identificationNationale) }}</div>
        <div class="col">{{ form_row(form.numeroTelephone, {label: 'Tél'}) }}</div>
    </div>
    <div class="row">
        {# <div class="col">{{ form_row(form.DateDeNaissance | date('d/m/Y' )) }}</div> #}
        <div class="col">{{ form_row(form.email) }}</div>
        <div class="col">{{ form_row(form.plainPassword, {label: 'Mot de passe'}) }}</div>
    </div>
        <div class="row">
        {# <div class="col">{{ form_row(form.DateDeNaissance | date('d/m/Y' )) }}</div> #}
        <div class="col">{{ form_row(form.agreeTerms, {label: "Termes d'agréement"}) }}</div>
    </div>
{{ form_end(form) }}
{% endblock %}
